tasks:
  build-deps:
    runner: razorci/ruby
    steps:
    - checkout
    - commands:
      dotnet sonarscanner begin /k:"dotnet" 
      /d:sonar.host.url="http://13.233.98.144:9000"  
      /d:sonar.login=${TOKEN}
      dotnet build
      dotnet sonarscanner end /d:sonar.login=${TOKEN}
